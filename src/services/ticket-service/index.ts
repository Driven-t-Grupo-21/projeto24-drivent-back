import { notFoundError } from '@/errors';
import eventRepository from '@/repositories/event-repository';

async function chooseTicketType() {
  return ''
}

const tiketService = {
  chooseTicketType,
};

export default tiketService;
